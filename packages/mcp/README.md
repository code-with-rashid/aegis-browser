# @aegis/mcp

Phase 2 stub. Will host the `McpClient` interface and a WebMCP adapter so external tool
servers can be exposed to the agent loop as additional actions. Not implemented or wired
into `@aegis/agent` yet — this package exists so the extension point is designed for from
the start.

Depends on `@aegis/shared`.
